Host.c:
- set packet type to either "TREE" or "STANDARD"
- set packetSenderType to 'H'

Switch.c:
- When sending the packet, if packet type = "TREE":
	- set packetSenderType to 'S'
	- set packetRootID = localRootID
	- set packetSenderChild
